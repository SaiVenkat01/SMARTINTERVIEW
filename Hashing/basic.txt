There are 2 types of collision resolution technique
1.Separate chaining
    -linked lists & sorted lists
2.open Addressing
    -linear Probing
    -Quadratic Probing
    -Double Hashing
    -Rehashing

Separate Chaining technique-
Bucket DS
used when there is dynamic data
Each chain is a sorting list

Hashing a String:
h(x)=for(int i=0;i<n-1;i++){ str(i)*pow(p,i+1)}
str(i)=>ascii of that character....